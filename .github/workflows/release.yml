# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: 🎯 Release To Production
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release Tag'
        required: false

jobs:
  publish:
    name: 🚀 Publish to Production
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout
        id: checkout
        uses: actions/checkout@v7
        with:
          submodules: true
          fetch-depth: 0

      - name: ⬇️ Checkout GitHub Pages Repo
        id: checkout_pages
        uses: actions/checkout@v7
        with:
          repository: yogendra/yogendra.github.io
          token: ${{ secrets.YMEGHIO_PAT }}
          path: public/github
          fetch-depth: 1

      - name: 🔓 Prepare cache directories
        run: |
          sudo mkdir -p /usr/local/bin ~/.cache ~/conda_pkgs_dir
          sudo chown -R $USER:$USER /usr/local/bin ~/.cache ~/conda_pkgs_dir

      - name: 📦 Cache Task CLI
        id: cache-task
        uses: actions/cache@v6
        with:
          path: /usr/local/bin/task
          key: ${{ runner.os }}-task-v3.38.0

      - name: Install Task CLI if missing from cache
        if: steps.cache-task.outputs.cache-hit != 'true'
        run: |
          sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b /usr/local/bin v3.38.0

      - name: 📦 Cache Tools & Dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.npm
            ~/.cache/pip
            /usr/local/bin/hugo
            /usr/local/bin/sass
          key: ${{ runner.os }}-tools-${{ hashFiles('Taskinit.yml') }}
          restore-keys: |
            ${{ runner.os }}-tools-

      - name: 📦 Cache Conda Environment & Packages
        id: cache-conda
        uses: actions/cache@v6
        with:
          path: |
            /usr/share/miniconda/envs/yogendra-me
            ~/conda_pkgs_dir
          key: ${{ runner.os }}-conda-env-${{ hashFiles('environment.yml') }}
          restore-keys: |
            ${{ runner.os }}-conda-env-

      - name: 📦 Cache Hugo Build Assets
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/hugo_cache
            resources
          key: ${{ runner.os }}-hugo-${{ hashFiles('assets/**', 'content/**', 'config/**') }}
          restore-keys: |
            ${{ runner.os }}-hugo-

      - name: 🐍 Setup Conda
        id: setup_conda
        uses: conda-incubator/setup-miniconda@v4
        with:
          activate-environment: yogendra-me
          environment-file: environment.yml
          pkgs-dirs: ~/conda_pkgs_dir
          auto-activate: false
          use-mamba: true

      - name: 🎬 Initialize Environment
        id: init
        run: task init

      - name: 🧱 Build Pages
        id: build
        run: |
          task version VERSION="${{ github.ref_name }}"
          task release:build

      - name: 🚀 Publish to Cloudflare
        id: publish_cloudflare
        run: task cloudflare:publish
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}


      - name: 🚀 Publish to Github
        id: publish_github
        env:
          YMEGHIO_PAT: ${{ secrets.YMEGHIO_PAT }}
        run: task github:publish        
      
    
      # - name: 🚀 Publish to Github
      #   id: publish_github
      #   uses: peaceiris/actions-gh-pages@v3
      #   with:
      #     personal_token: ${{ secrets.YMEGHIO_PAT }}          
      #     external_repository: yogendra/yogendra.github.io
      #     publish_branch: main
      #     publish_dir: ./public/github

      - name: 🔏 Update and Close Testing Ticket
        id: close_ticker
        uses: actions/github-script@v9
        with:
          script: |
            const label = 'beta-testing';
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'all',
              labels: [label],
              per_page: 5
            });

            if (issues.length > 0) {
              const issueNumber = issues[0].number;
              const tag = context.ref.replace('refs/tags/', '');
              const runUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
              
              const comment = `## ✅ Production Release Complete\n\n- **Release Tag:** \`${tag}\`\n- **Production URL:** [https://yogendra.me](https://yogendra.me)\n- **Release Build:** [View Logs](${runUrl})\n- **Status:** Closed and Released to Production.`;

              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                body: comment
              });

              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                state: 'closed',
                state_reason: 'completed'
              });

              if (!issues[0].locked) {
                await github.rest.issues.lock({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  lock_reason: 'resolved'
                });
              }
              console.log(`Updated, closed, and locked issue #${issueNumber}`);
            } else {
              console.log('No beta-testing issue found.');
            }

      - name: 📢 Generate Summary
        id: summary
        run: |
          echo "## 🎊 Production Release Successful" >> $GITHUB_STEP_SUMMARY
          echo "- **Site URL:** [https://yogendra.me](https://yogendra.me)" >> $GITHUB_STEP_SUMMARY
          echo "- **Release Tag:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Build Status:** Success ✅" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "The beta testing issue has been updated and closed." >> $GITHUB_STEP_SUMMARY
